# Cloudflare DDNS Updater example environment file
# Rename to `.env` (kept out of version control) and fill in the values you need.
# Lines beginning with # are comments or disabled examples.

# ---------------------------------------------------------------------------
# Authentication (choose exactly ONE method)
# ---------------------------------------------------------------------------
# Global API Key + Email (legacy, broad access – rotate if exposed)
CLOUDFLARE_EMAIL=you@example.com
CLOUDFLARE_API_KEY=

# OR (recommended) a scoped API Token with DNS:Edit for just the needed zone(s)
# CLOUDFLARE_API_TOKEN=

# ---------------------------------------------------------------------------
# Configuration Modes
# ---------------------------------------------------------------------------
# 1. Single Zone (legacy)
# Uncomment to manage one record (default record name = zone if not set)
# CLOUDFLARE_ZONE_NAME=example.com
# CLOUDFLARE_RECORD_NAME=home.example.com

# 2. Multi-Zone (multiple zones, one record each or explicit per-zone records)
# CLOUDFLARE_ZONE_NAMES=example.com,example.net
# CLOUDFLARE_RECORD_NAMES=home.example.com,office.example.net   # must match zone count when provided
# CLOUDFLARE_RECORD_NAME=dynamic.example.com                    # (alternative) single record name applied to all zones

# 3. Single Zone, Multiple Records (apex + subdomains) NEW
# Provide a single zone name plus a list of record names; the zone is replicated internally.
# CLOUDFLARE_ZONE_NAME=example.com
# CLOUDFLARE_RECORD_NAMES=example.com,home.example.com,office.example.com

# ---------------------------------------------------------------------------
# Common Optional Settings (apply to every generated record update task)
# ---------------------------------------------------------------------------
# CLOUDFLARE_RECORD_TYPE=A        # or AAAA
# CLOUDFLARE_TTL=300              # seconds
# CLOUDFLARE_PROXIED=false        # set true to enable Cloudflare proxy (orange cloud)
# DDNS_INTERVAL=300               # if set, run continuously every N seconds
# DDNS_DRY_RUN=false              # if true, show actions without calling Cloudflare

# ---------------------------------------------------------------------------
# Precedence Summary
# ---------------------------------------------------------------------------
# Zones priority: --zones CLI > CLOUDFLARE_ZONE_NAMES > CLOUDFLARE_ZONE_NAME
# Records priority (per final zone list):
#   --records > --record > CLOUDFLARE_RECORD_NAMES > CLOUDFLARE_RECORD_NAME > default(zone)
# Special case: single zone + CLOUDFLARE_RECORD_NAMES => multi-record updates in that zone.

# ---------------------------------------------------------------------------
# Security Tips
# ---------------------------------------------------------------------------
# - Prefer CLOUDFLARE_API_TOKEN over global key (least privilege).
# - Never commit a real key/token. Rotate immediately if exposed.
# - Restrict token permissions to: Zone.DNS Edit (and Zone.Zone Read if required by Cloudflare UI).

# End of file
